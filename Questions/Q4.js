// Write a function that takes an array of strings and uses the forEach() method to print each string with "Hello, " prepended to it.

const arr = ["Parv", "Harry", "Ron", "Hagrid", "Fang"];

arr.forEach((ele) => {
  console.log(`Hello ${ele}!`);
});
